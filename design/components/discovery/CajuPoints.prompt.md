**CajuPoints** — points earned by feeding the Brain knowledge (feedback, quizzes, photos, corrections), never by opening the app. Show a `+N` delta right after a contribution.

```jsx
<CajuPoints value={1240} size="lg" unit="puntos" />
<CajuPoints value={30} delta={30} chip size="sm" />
```
