# Features Pendientes / Gaps Conocidos

Documento vivo de nivel **implementación** — distinto de [product-decisions.md](product-decisions.md), que registra decisiones de producto abiertas. Acá se anota todo lo que quedó simulado, a medias, o directamente sin construir mientras se implementaba cada spec, para no perderlo de vista.

Convención: cuando un gap se cierra, se mueve a "Resueltos" con fecha y commit, no se borra — así queda historial de qué se decidió postergar y cuándo se resolvió.

## Por spec

### SPEC-001 — Living Map
- [x] Chips "Cerca" y "Abierto ahora" filtran de verdad, con geolocalización y horarios reales — resuelto, ver abajo.
- [x] FAB "Mi ubicación" funcional (recentra el mapa con geolocalización real) — resuelto, ver abajo.
- [x] El chip de barrio ya no muestra "Palermo" fijo sin importar dónde esté el usuario — resuelto, ver abajo (2026-07-07).
- [ ] FAB "Capas" sigue decorativo — decisión deliberada, no un gap: el propio spec lo marca como "Capas futuras" y sus Open Questions (heatmap, clusters, pines inteligentes, vista 3D, AR) dejan sin definir qué sería una capa real. Implementar algo ahora sería inventar alcance que el spec no fija.

### SPEC-002 — Conversation Experience
- [x] Streaming real de la respuesta del Brain (antes llegaba completa de una sola vez) — resuelto, ver abajo.

### SPEC-004 — Knowledge Acquisition
- [ ] Reel/TikTok sigue como simulación honesta y explícitamente fuera de alcance de SPEC-015 — requiere scraping/API oficial de una plataforma de terceros, una decisión legal y de costo real, no técnica.
- [x] El input de link (Reel/TikTok) ya envía el texto pegado al Brain — resuelto, ver abajo.
- [x] Entry points del spec: Share Sheet nativo del OS y captura "desde la conversación" — resuelto, ver abajo.
- [x] Texto libre ("Nota") — resuelto, ver abajo.
- [x] Foto y Voz ([SPEC-015](specs/SPEC-015-real-knowledge-ingestion.md)) — resuelto, ver abajo.
- [x] El mic de Living Map ("Aportar por voz") navegaba a Conversación en vez de abrir el paso de Voz real — resuelto, ver abajo (2026-07-07).

### SPEC-005 — Recommendation Engine
- [ ] Sin señales externas (curadores/foodies) — el catálogo son 6 restaurantes hand-authored (subieron de 3), no hay ingesta real de fuentes (eso es SPEC-004 a nivel de fuente, no de captura de usuario).
- [x] Context Chips con filtro real (date/work/saved) — resuelto, ver abajo.
- [x] Catálogo ampliado de 3 a 6 restaurantes — resuelto, ver abajo.

### SPEC-006 — Memory Engine
- [ ] Solo modela guardados + ADN (tags planos) + puntos + colecciones. Los tipos de memoria más ricos del spec (hábitos, patrones, contextual, temporal) no están implementados.

### SPEC-007 — Trust Engine
- [x] Consensus y Contradictions — resuelto, ver abajo.

### PRD-010 — Onboarding
- [x] Flujo de primer uso (Bienvenida → 2-3 preguntas opcionales → Living Map) — resuelto, ver abajo.
- [ ] El mecanismo exacto de OTP (SMS/WhatsApp/otro) sigue sin definir — sigue siendo la "Decisión abierta #1" en [product-decisions.md](product-decisions.md). El *momento* en que se dispara ya se especificó, ver siguiente punto.

### SPEC-013 — Deferred Identity (nueva)
- [x] Identidad anónima server-side + "Guardá tu Brain" (teléfono + OTP) — resuelto, ver abajo.

### SPEC-014 — Compare Experience (nueva)
- [x] Comparar 2-3 restaurantes con una conclusión real (nunca una tabla) — resuelto, ver abajo.

### SPEC-016 — Notifications (nueva)
- [x] Infraestructura real de push (VAPID + service worker + subscriptions) y 2 de los 6 tipos con disparador real y sincrónico (Nuevos lugares, Cambios importantes) — resuelto, ver abajo.
- [x] Scheduler real (basado en timestamps persistidos, no un `setInterval` que finge tiempo) para los otros 4 tipos (Recomendaciones, Recordatorios, Feedback pendiente, Eventos) — resuelto, ver abajo.
- [x] Eventos dejó de ser un único fixture hardcodeado sin forma de agregar uno real — CRUD real vía Admin CMS — resuelto, ver abajo (2026-07-07).
- Planning (CP-071) quedó deliberadamente sin especificar — no se ve útil para el uso actual de la app.

### SPEC-017 — Curators & Sources (nueva)
- [x] Curador como Source Entity con reputación real por dominio (cocina), recalculada solo con evidencia real — resuelto, ver abajo.

### SPEC-018 — Admin CMS (nueva)
- [x] CMS como "otro cliente del Brain" (nunca lógica paralela), con un tercer nivel de identidad (allowlist de operadores) — resuelto, ver abajo.

### SPEC-019 — User Contribution Moderation (nueva)
- [x] Cola de moderación real para lo que un usuario común le enseña al Brain (Nota/Foto/Voz/conversación) — antes solo llegaba a su `memory.json` privado, nunca al catálogo compartido, ni siquiera tras revisión — resuelto, ver abajo.

### Estructural / fuera de un spec puntual
- [x] Multi-tenant real por `userId` anónimo (antes un solo usuario `u1` hardcodeado) — resuelto vía SPEC-013, ver abajo. Sigue sin login tradicional (contraseña/email) por decisión de producto explícita — identidad diferida, no ausente.
- [x] Auditoría de datos hardcodeados/fake y robustez del Brain (fallback silencioso de Claude, conteos de comunidad inventados, default fantasma en Feedback, IDs de turno no persistentes) — resuelto, ver abajo (2026-07-07).
- [ ] Persistencia en un JSON file, no en una base de datos real (decisión de scope explícita, ver el plan original).
- [ ] PRD-015 (Planning Engine): sin spec ni implementación, deliberadamente pospuesto por decisión de producto — no se lo considera útil para el uso actual de la app.
- [ ] Usuario demo fake ("Lucas", 1.240 puntos, aportes inventados) sigue apareciendo en la primera instalación (`memoryStore.ts` `seededDemoState`) — decisión explícita de dejarlo así por ahora (auditoría del 2026-07-07), no un descuido.
- [x] Preparación del repo para deploy real (Railway backend + Vercel PWA): `DATA_DIR` configurable para apuntar a un volumen persistente, CORS restringible a un origen real, configs de build/rewrite — resuelto, ver [deploy.md](deploy.md) y abajo (2026-07-07). El deploy en sí (crear los proyectos, cargar secrets reales) queda para que lo ejecute el usuario siguiendo ese runbook — son acciones sobre sus propias cuentas de Railway/Vercel.

## Resueltos

- **2026-07-05** — Nota de texto libre en Knowledge Capture ahora es genuinamente Claude-backed (`extractNoteKnowledge`), en vez de una respuesta plantilla para cualquier tipo de aporte. Commit `480a371`.
- **2026-07-05** — Bug: el `sub` de la Brain Card no pasaba por `highlightText()`, mostrando `**asteriscos**` literales en vez de negrita. Commit `347ba18`.
- **2026-07-05** — Context Chips "Para una cita", "Trabajar" y "Guardados" ahora filtran de verdad contra el Recommendation Engine (antes eran puramente visuales). Commit `6c43a31`.
- **2026-07-05** — Catálogo ampliado de 3 a 6 restaurantes (Nonna Emma, Terraza Norte, Brote), mismo formato hand-authored. Da variedad real a diversify/search/trust (ahora sí aparece un ejemplo de confianza "low") y más de una opción por filtro de contexto. Sigue sin ser ingesta real de fuentes externas. Commit `3af1405`.
- **2026-07-05** — Trust Engine ahora detecta contradicciones reales entre fuentes (`claim` + ejes de contradicción determinísticos) y las nombra en el rationale en vez de promediarlas en silencio; Consensus ya existía de forma implícita (diversidad de `kind`), quedó documentado como tal. De paso se descubrió que `trustRationale` se calculaba pero nunca llegaba a la UI — ahora se muestra en la ficha del restaurante. Anafe quedó con una contradicción real de ejemplo (ahora "Señales en conflicto"). Commit `55947e1`.
- **2026-07-05** — Onboarding (PRD-010) implementado: Bienvenida → 3 preguntas opcionales (chip-based) → Living Map. Las respuestas se guardan como DNA tags reales y ya afectan recomendaciones reales (verificado: elegir "Palermo" hizo que la próxima Brain Card mencionara "está en tu barrio"). No toca el método de autenticación (sigue como decisión abierta separada). Commit `15015af`.
- **2026-07-05** — SPEC-014 (Compare) implementado: nueva función `compareRestaurants` en `claudeClient.ts` + ruta `/api/compare`, wireada al chip "Comparar con otro" en Conversation. Siempre termina en una conclusión (`recommendedId` real o `null` honesto si no hay evidencia suficiente), nunca una tabla. Verificado con `curl` contra el Brain real en dos casos (empate y ganador claro). Commit `5d22df7`.
- **2026-07-05** — SPEC-015 (Foto y Voz) implementado: Foto usa el input multimodal nativo de Claude (`extractPhotoKnowledge`, sin proveedor de OCR) para identificar el restaurante y resumir solo lo legible en la imagen; Voz usa la Web Speech API del navegador para transcribir y reusa el mismo `extractNoteKnowledge` que Nota. Ambos flujos verificados en vivo contra el Brain real (Voz con una nota hablada sobre Anafe, Foto con una imagen de menú sintetizada para Osaka), con resúmenes correctos y puntos otorgados. Commit `5d22df7`.
- **2026-07-06** — SPEC-013 (Deferred Identity) implementado: `memoryStore.ts` pasó de un usuario `u1` hardcodeado a estar keyeado por `userId` (Map de filas independientes); un nuevo `identityMiddleware` lee `X-Caju-User-Id` y lo adjunta a `req.userId` sin bloquear rutas de solo catálogo; rutas que leen/escriben memoria personal (`saved`, `dna`, `collections`, mensajes, capturas) exigen el header vía `requireUserId` (400 si falta). El cliente PWA genera y guarda un UUID anónimo en `localStorage` (`getAnonId()`) y lo manda en cada request — nunca lo emite el Brain. Conversation y Knowledge Capture (los dos únicos flujos con costo real de Claude) quedaron detrás de un rate limit diario por usuario anónimo (15 mensajes / 8 capturas); al agotarse, el Brain responde `429 { error: 'anon_limit_reached', requiresSync: true }` en vez de dejar pasar costo ilimitado sin ninguna identidad detrás. "Guardá tu Brain" en Profile adjunta un teléfono a la fila anónima existente vía OTP de un solo uso (sin vendor de SMS conectado todavía — el código se devuelve directo en la respuesta para que el flujo sea end-to-end testeable); un teléfono ya vinculado a otro `userId` vuelve como conflicto explícito en vez de fusionar filas en silencio. Verificado con `curl` (aislamiento de `saved`/`dna`/`collections` entre dos `userId`, el límite 15→429, y las tres ramas de OTP: código incorrecto, código correcto, conflicto de teléfono) y en el navegador real (flujo completo de "Guardá tu Brain" hasta "Tu Brain está protegido — sincronizado con +541155667788."). El mecanismo real de envío de SMS/WhatsApp sigue como decisión abierta (ver product-decisions.md). Commit `b4a9e7e`.
- **2026-07-06** — SPEC-018 (Admin CMS) implementado: prerequisito real resuelto primero — el catálogo pasó de `RAW_RESTAURANTS` hardcodeado en TypeScript a `brain/data/catalog.json` (mismo patrón de `memoryStore.ts`; `catalogSeed.ts` solo bootstrapea el archivo la primera vez), con `createRestaurant`/`updateRestaurant`/`addSourceToRestaurant` nuevos y `trust`/`trustRationale` recalculados en cada lectura en vez de cachearse (ya no es seguro cachear una vez que el CMS puede escribir). Tercer nivel de identidad vía `requireOperator` (header `X-Caju-Operator-Token` contra `ADMIN_TOKEN`), completamente separado de la identidad de usuario final (SPEC-013) — nunca alcanza con un teléfono verificado común. Nuevas rutas `/api/admin/*`: overview de solo lectura del catálogo completo con confianza y rationale (algo que ningún usuario final ve junto), alta/edición directa de restaurantes, y `/api/admin/analyze` + `/api/admin/restaurants/:id/sources` para el caso "Analizá esta lista/@curador" — un operador pega texto real ya leído (nunca se scrapea Instagram/TikTok), `analyzeCuratorContent` (nueva función en `claudeClient.ts`, mismo patrón grounded que `extractNoteKnowledge`) identifica qué restaurantes reales menciona con qué claim y peso sugerido, y **nunca se aplica solo** — el operador confirma sugerencia por sugerencia (Confirmación Inteligente, CP-009) antes de que se sume como Source real. Fusionar entidades quedó deliberadamente sin implementar (el propio spec lo deja como Open Question sin mecanismo resuelto). UI mínima nueva en `/admin` dentro de la misma PWA (gate por token, tabla de confianza, analizar+confirmar, alta de restaurante) — excluida del gate de Onboarding, ya que un operador no es un usuario final. Verificado con `curl` (auth 401 sin token/con token incorrecto, alta y edición de restaurantes, análisis grounded que correctamente distinguió a Osaka/Anafe reales de "La Fonda del Puerto" inventado) y en el navegador real (login con token, tabla de confianza completa incluida la contradicción de Anafe, análisis de un texto sobre Cuervo Café que subió su confianza de `mid` a `high` al confirmar la fuente en vivo, alta de un restaurante nuevo reflejada al instante en la tabla). SPEC-017 (Curators & Sources) queda desbloqueado para una futura sesión. Commit `589fe5a`.
- **2026-07-06** — SPEC-017 (Curators & Sources) implementado: un curador deja de ser un string repetido en `sources[]` y pasa a ser una entidad real (`brain/src/curators/curatorStore.ts`, JSON-persistido igual que `memoryStore.ts`/`catalogStore.ts`), identificada por el mismo handle que ya se usaba como `Source.name` — sin ID nuevo, sin migración. La reputación se trackea por dominio (cocina) vía `{ sustained, contradicted }` y nunca se asigna una vez: `getCatalog()` ahora reemplaza el `weight` de cada fuente `curator` por su reputación efectiva (`getEffectiveWeight`) antes de pasarlas a `computeTrust`, y `addSourceToRestaurant` decide si una fuente nueva sostiene o contradice reusando exactamente `detectContradiction` (ahora exportada de `trustEngine.ts`) y el chequeo de diversidad de `kind` que ya existía — nunca lógica paralela. Un curador sin corroboración real (ni sostenida ni contradicha) nunca mueve su reputación por una afirmación aislada. Se hizo un backfill único desde el catálogo semilla al primer boot: `@buenospaladaires` arrancó con evidencia sostenida real en Nikkei/Café de especialidad/Trattoria italiana, `@salt_argentina` con la contradicción real de Anafe en Bodegón moderno. Bug real encontrado y corregido durante la verificación en vivo: la UI de "Analizar contenido" inicialmente atribuía toda fuente confirmada a un placeholder genérico ("Operador (contenido analizado)") en vez del handle real del curador pegado — mezclaba la reputación de curadores distintos en un mismo bucket sin sentido; se agregó un campo obligatorio de handle en el Admin CMS antes de poder analizar o confirmar. Verificado con `curl` y en el navegador: el peso efectivo de `@buenospaladaires` en Osaka bajó de `strong` (hardcodeado) a `medium` por reputación insuficiente, y volvió a `strong` tras 3 confirmaciones netas sostenidas; un handle de prueba nuevo (`@test_verificacion`) apareció correctamente con su propio historial aislado tras confirmar una sugerencia real desde el Admin CMS. Reputación visible solo a operadores (no a usuarios finales) y umbral exacto de sostenidas/contradichas quedan como decisiones de implementación explícitas, no de producto (el propio spec las deja como Open Questions sin resolver); "curador vs. curador" contradictorio tampoco se resuelve acá — mismo principio de SPEC-007 (nunca elegir arbitrariamente), caso específico pendiente. Commit `1c92574`.
- **2026-07-06** — SPEC-016 (Notifications) implementado parcialmente, con la infraestructura real completa y 2 de los 6 tipos con disparador genuino: `web-push` + VAPID (`brain/src/notifications/pushStore.ts` persiste subscriptions por `userId`, mismo patrón JSON que el resto; `pushSender.ts` envía y se auto-limpia si una subscription expiró/es inválida — 404/410). El service worker pasó de `generateSW` (autogenerado, sin push) a `injectManifest` con un `src/sw.ts` propio (`push` → `showNotification` con el motivo en el body, `notificationclick` → navega a la pantalla real) — requirió un `tsconfig.sw.json` separado con lib `WebWorker` en vez de `DOM`, porque ambos conjuntos de tipos globales chocan en el mismo programa de TS. El opt-in se pide en contexto real (sección "Notificaciones" en Profile, visible recién después de guardar al menos un lugar) — nunca en el primer uso. Dos disparadores reusan señales que el Recommendation/Trust Engine ya calculaban, sin lógica nueva: "Nuevos lugares" corre `matchDna` (extraído de `scoreRestaurant`, ahora exportado) contra cada usuario suscripto justo cuando un operador crea un restaurante desde el Admin CMS; "Cambios importantes" compara el `trust` antes/después de que un operador confirme una fuente nueva, y solo avisa a quienes ya tenían ese lugar guardado (`getUsersWhoSaved`, nuevo en `memoryStore.ts`). Los otros 4 tipos (Recomendaciones, Recordatorios, Feedback pendiente, Eventos) necesitan un disparo por tiempo transcurrido, no por una acción que ya ocurre en el request — sin un scheduler real en el proyecto, implementarlos ahora hubiera significado fingirlos con un `setInterval` de proceso, que muere con el dev server y da una falsa sensación de estar resuelto; quedan documentados como gap explícito, no simulados. Verificado con `curl` contra un usuario y una subscription (endpoint falso, a propósito) reales: el pipeline completo corrió de punta a punta — crear un restaurante que matchea ADN disparó un intento real de `web-push.sendNotification` (falló por DNS al ser un endpoint inventado, exactamente lo esperado), y agregar una fuente que subió el `trust` de un lugar guardado disparó el segundo aviso. Verificado en el navegador: el Service Worker registra y queda `active`, y el build de producción (`vite build`) genera correctamente `dist/sw.js` en modo `injectManifest`. No se pudo ejercitar el permiso nativo del navegador end-to-end porque el navegador headless de este entorno tiene `Notification.permission` en `"denied"` por defecto y no hay forma de simular el click de "Permitir" — limitación de testing real, documentada en vez de asumida como probada. Commit `f74554a`.
- **2026-07-06** — SPEC-002 (streaming real de la Conversation) implementado: en vez de duplicar la llamada a Claude (una para decidir, otra para redactar) o abandonar el schema JSON grounded que ya validaba `restaurantIds`/`chips`, se mantiene una sola llamada con `output_config.format` pero invocada vía `client.messages.stream()` — un nuevo `createStringFieldExtractor` (`brain/src/llm/streamingJsonField.ts`) busca el marcador `"reply":"` dentro del JSON crudo que todavía se está generando y va extrayendo, char a char, solo el texto plano de ese campo (soporta escapes `\"`, `\\`, `\n`, `\t`, `\r` partidos entre chunks), sin asumir que `reply` sea la primera propiedad del schema. `interpretQuery` ahora acepta un `onDelta` opcional; `restaurantIds`/`chips` se siguen leyendo y groundeando solo una vez completo el JSON (`stream.finalMessage()`), mismo chequeo que antes. La ruta `/api/messages` pasó de `res.json()` a un body ND-JSON chunkeado (`res.write()` repetido, sin SSE completo): líneas `{"type":"delta","text":"..."}` seguidas de una final `{"type":"done","turn":{...}}`. `BrainClient.sendMessage` ganó un segundo parámetro `onDelta?` (cambio de firma, no aditivo — se confirmó que `Conversation.tsx` es el único consumidor en ambos lados); `httpBrainClient` lee el stream con `res.body.getReader()` y parsea línea por línea; `mockBrainClient` simula entrega palabra por palabra con timeouts cortos, comentado explícitamente como simulado (mismo patrón de honestidad que otros mocks). `Conversation.tsx` inserta una burbuja placeholder en el primer chunk y la va creciendo en vivo, reemplazándola por el turno final ya groundeado al terminar. Verificado con `curl` contra el Brain real, dos veces en sesiones distintas: los chunks reconstruidos concatenados coinciden exactamente con el `text` del turno final. Verificado en el navegador ejecutando `brain.sendMessage(...)` directamente en consola (mismo código de producción, invocado sin pasar por un click real): deltas progresivos en orden y turno final correctos, sin duplicación. La verificación por click real en la UI quedó inconclusa: tras una caída transitoria del clasificador de seguridad del entorno de preview a mitad de sesión (bloqueó `preview_*` y hasta `curl` vía Bash), el entorno de preview quedó en un estado inconsistente — los logs de consola y red mostraban entre 6 y 8 ciclos duplicados de "conectado a Vite"/"descargá React DevTools" por cada carga de página, y una recarga completa automática apenas resolvía cada mensaje, incluso reiniciando ambos servidores y desregistrando el Service Worker. Todo apunta a contextos de navegador duplicados que sobrevivieron a la caída, no a un bug del código: cada ejecución individual (aunque duplicada) reconstruía el texto correctamente, y el único punto no cubierto por otra vía (curl + consola) es ver la burbuja crecer en pantalla real por un click real. Documentado con honestidad en vez de forzar una verificación con una herramienta inestable. Commit `540e541`.
- **2026-07-06** — SPEC-001 (Living Map): los chips "Cerca" y "Abierto ahora" pasan a filtrar de verdad, y el FAB "Mi ubicación" pasa a ser funcional. "Abierto ahora": cada restaurante ganó `openHours` reales (turnos por día de semana, coherentes con su carácter — Cuervo Café de mañana, Osaka/Nonna Emma solo de cena, Terraza Norte findes) y una función determinística `isOpenNow()` (`brain/src/geo/geo.ts`, sin LLM, misma filosofía que el Trust Engine) que calcula la hora real en Buenos Aires sin importar la zona horaria del servidor; el quickFact "Abierto ahora"/"Cerrado ahora" pasó de estar hardcodeado en 3 restaurantes a calcularse en cada lectura de `getCatalog()`, igual que `trust`, nunca guardado. "Cerca": nueva función `haversineKm()` + un radio real de 2km (umbral placeholder, documentado igual que otros del proyecto); si nadie cae dentro del radio, hace fallback a los más cercanos ordenados en vez de mostrar cualquier cosa — nunca vacío (CP-018). El cliente pide geolocalización real solo en contexto (nunca al abrir la app): un chip nuevo "Usar mi ubicación" aparece únicamente mientras no hay ubicación, el chip "Cerca" la pide él mismo si hace falta, y el FAB "Mi ubicación" la vuelve a pedir y recentra el mapa (`LivingMapCanvas` pasó a exponer un `recenter()` imperativo vía `forwardRef`, ya que antes solo fijaba el centro una vez al crear el mapa). El FAB "Capas" queda deliberadamente sin implementar — el propio spec lo marca "futuras" y sus Open Questions (heatmap, clusters, 3D, AR) no definen qué sería, así que no hay alcance real que cablear todavía. Verificado con `curl` (lunes 17:00 en Buenos Aires: solo Cuervo Café "abierto ahora", confirmado matemáticamente contra los horarios reales; filtro `near` con coordenadas de Villa Crespo devolvió exactamente los 3 lugares dentro de 2km; coordenadas lejanas activaron el fallback a más cercanos) y en el navegador con `navigator.geolocation.getCurrentPosition` mockeado (ya que este entorno no permite simular el prompt nativo): el chip "Usar mi ubicación" desapareció al conceder la ubicación, y tocar "Cerca" disparó un request real con esas coordenadas exactas, devolviendo la Brain Card y los pines correctos sin errores de consola. Commit `7577b66`.
- **2026-07-07** — SPEC-016 (scheduler real para los 4 tipos restantes de notificaciones) implementado: en vez de fingir el paso del tiempo con un `setInterval` que resetea su estado cada vez que el proceso de dev se reinicia, cada condición se evalúa contra un timestamp real persistido en `memoryStore.ts` (`lastActiveAt`, `savedAt`, `feedbackGiven`, `notifiedAt`), así que un reinicio del servidor solo hace que el próximo tick vuelva a evaluar el tiempo real transcurrido — nada arranca de cero. "Recomendaciones" reusa `getRecommendations()` tal cual (la misma Brain Card que vería el usuario en la app) una vez que pasan 3 días reales sin actividad y existe ADN real; "Recordatorios" y "Feedback pendiente" — que el propio spec trata como el mismo disparador — se resuelven juntos: `setSaved()` ahora guarda un `savedAt` real por restaurante, `/feedback` marca `feedbackGiven` cuando llega un `restaurantId` real (antes se descartaba), y `getPendingFeedback()` devuelve los lugares guardados sin feedback aún; "Eventos" ganó un campo `whenAt` real (ISO) en `MapEvent` —antes `when` era solo una etiqueta de display tipo "sáb", no una fecha real contra la que programar nada— y se dispara dentro de una ventana de anticipación real antes de la fecha. Los umbrales exactos (3 días de inactividad, 24hs desde el guardado, 24hs de anticipación para eventos, cooldown de 24hs para no repetir el mismo push) son decisiones de placeholder explícitas, igual que `NEAR_RADIUS_KM`/`STRONG_MATCH_THRESHOLD` — el spec deja "frecuencia máxima" y "con cuánta anticipación avisar" como Open Questions sin resolver. Un tick (`brain/src/notifications/scheduler.ts`, cada 60s) itera solo a los usuarios con una subscription real de push; cada uno de los 3 chequeos corre de forma independiente (una falla en uno no bloquea a los demás para el mismo usuario). Verificado ejecutando `runNotificationTick()` directamente contra un usuario de prueba con datos reales manipulados a propósito (guardado hace 25hs sin feedback, última actividad hace 4 días, ADN real, y la fecha del evento corrida temporalmente a la ventana de anticipación para no esperar días reales): los 3 tipos dispararon en el mismo tick un intento real de `web-push.sendNotification` (falló por una clave p256dh inventada a propósito, mismo patrón de "falla esperada con datos falsos" que la verificación original de SPEC-016), y un segundo tick inmediato no volvió a disparar nada gracias al cooldown persistido — confirmando que el estado sobrevive y se respeta entre ticks. Fuera de alcance, señalado para otra sesión: el nudge de feedback en Profile.tsx sigue hardcodeado a "¿Cómo estuvo Osaka?" en vez de usar `getPendingFeedback()` real — es un gap preexistente en la UI, no en el disparador de la notificación push en sí. Commit `996fe67`.
- **2026-07-07** — Profile.tsx dejó de mostrar datos fijos: el nudge "¿Cómo estuvo...?" y la sección "Tus aportes" eran fixture hardcodeado (`CONTRIBUTIONS` y "Osaka" fijos) sin relación con el usuario real, a pesar de que el Brain ya calculaba ambas cosas (`contributions` en `memoryStore.ts`, y `getPendingFeedback()` recién agregado para el scheduler de SPEC-016). Nueva ruta `GET /api/activity` (`brain/src/routes/activity.ts`) junta ambas señales — el pending feedback se resuelve contra el catálogo real para devolver el nombre del restaurante, no solo su id — y un nuevo método `BrainClient.getActivity()` lo expone en ambos clientes (mock con su propio `savedAt`/`feedbackGiven` en memoria, misma forma que el Brain real). El nudge ahora muestra el lugar guardado más antiguo sin feedback (o desaparece si no hay ninguno) y pasa su `restaurantId` real al overlay de Feedback — que hasta ahora recibía el click sin contexto y asumía "osaka" a ciegas — vía un nuevo parámetro opcional en `openOverlay()` (`overlayRestaurantId` en el store). La sección "Tus aportes" ahora renderiza `contributions` real con un formateador `timeAgo()` en vez de strings "hace X días" hardcodeados. Verificado en el navegador contra el Brain real: el nudge mostró "¿Cómo estuvo Osaka?" (el guardado más antiguo sin feedback), completar el flujo de Feedback sumó 45 puntos reales (1.240 → 1.285) y creó una fila real en `contributions`, y al volver a Profile el nudge pasó a "¿Cómo estuvo Cuervo Café?" — confirmado también contra `/api/activity` directamente con `curl` antes y después del envío. Commit `ab47225`.
- **2026-07-07** — SPEC-004 (dos entry points reales + un bug preexistente) implementado. **Share Sheet nativo del OS**: nuevo `share_target` (GET, `title`/`text`/`url`) en el manifest de la PWA registra a CajuEat como destino real en el share sheet del sistema (Instagram → Compartir → CajuEat); una nueva pantalla `/share` recibe el payload, lo deja precargado en el input de link de Knowledge Capture (nunca se auto-envía — el usuario sigue confirmando con un solo tap, igual que si lo hubiera pegado a mano) y redirige a Home. **Bug real corregido de paso**: el botón "Enviar" del input de link llamaba a `start('link')` sin pasar el valor pegado — se perdía en silencio incluso para un aporte manual. Ahora se envía real, y `capture.ts` deja de descartarlo: el link real queda en el label de la contribución (`Aportaste un link: <url>`) — sigue sin scrapearse el contenido (esa es la decisión legal/de costo ya documentada), pero ya no se tira el link en sí. **"Desde conversación"**: nueva función `extractConversationKnowledge` en `claudeClient.ts`, deliberadamente más conservadora que `extractNoteKnowledge` — instruida para devolver vacío ante cualquier pregunta o pedido de recomendación, y solo reconocer una afirmación real de primera mano. La ruta `/api/messages` la invoca solo cuando el texto crudo del usuario menciona por nombre un restaurante real del catálogo (gate local sin costo, no el resultado de `interpretQuery` — se probó primero con ese gate y fallaba el caso principal, porque "conté que fui a Osaka" no genera ninguna recomendación real que agrupar); si identifica conocimiento nuevo, registra una contribución real (`recordContribution`, 30 puntos, mismo monto que Nota/Foto/Voz) sin interrumpir la conversación con una confirmación aparte — el turno de respuesta lleva `learnedAbout`/`learnedPoints` y la UI muestra una etiqueta discreta ("Le enseñaste algo a Caju sobre X") debajo del mensaje, más la actualización real de Caju Points. El mock replica el mismo comportamiento con una heurística local (sin LLM): nunca ante una pregunta, solo ante una frase con tono de experiencia propia que nombra un restaurante real. Verificado con `curl` contra el Brain real: "Fui a Osaka... el omakase estuvo buenísimo" generó `learnedAbout: "Osaka"` y una contribución real de 30 puntos; la pregunta control "¿Osaka es bueno para ir en pareja?" no generó ninguna (cero falsos positivos). Verificado en el navegador: compartir un link simulado vía `/share?url=...` dejó el input de Knowledge Capture precargado, y confirmar con "Enviar" disparó un `POST /api/capture` real que quedó registrado en `/api/activity` con la URL real incluida en el label. El envío real de un mensaje de Conversation desde la UI (no solo `curl`) también confirmó, contra `/api/activity`, un aporte real "Le enseñaste algo a Caju sobre Osaka" — la re-aparición de la misma caída transitoria de tooling documentada en la entrada de SPEC-002 (un reload automático del entorno de preview justo después de la respuesta) impidió confirmar visualmente la etiqueta en pantalla en esa misma pasada, pero el dato que la etiqueta renderiza (`turn.learnedAbout`) llegó correcto de punta a punta — mismo patrón de `restaurants`/`chips`, ya verificado visualmente antes. Commit `7719d3b`.
- **2026-07-07** — [SPEC-019](specs/SPEC-019-user-contribution-moderation.md) (User Contribution Moderation, spec nueva) implementado. Surgió de una pregunta directa del usuario sobre dónde vive la memoria compartida — la respuesta reveló un gap real, no documentado hasta ahora: `addSourceToRestaurant` (la única función que escribe un `Source` real en el catálogo compartido) solo se llamaba desde `admin.ts`; los cinco caminos de captura de un usuario común (Nota/Foto/Voz/link/"desde conversación") únicamente escribían en su propio `memory.json` privado vía `recordContribution` — lo que un usuario le enseñaba al Brain sobre un restaurante nunca llegaba a otro usuario, ni siquiera después de que un operador lo revisara, porque no había nada que revisar. En vez de auto-agregar directo al catálogo (rompería la garantía del Trust Engine de "nunca confiar a ciegas", SPEC-007) se reusó tal cual el patrón de Confirmación Inteligente que ya existía para curadores en el Admin CMS (SPEC-018): un tercer store nuevo (`brain/src/moderation/pendingContributionsStore.ts`, mismo patrón JSON-persistido que `memoryStore.ts`/`catalogStore.ts`, deliberadamente sin `userId` — el operador revisa el claim y el restaurante, nunca quién lo escribió, mismo minimalismo de identidad de SPEC-013) encola cada aporte con `learned` real de Nota/Foto/Voz (`capture.ts`) y "desde conversación" (`conversation.ts`) además de registrar la Contribution privada de siempre — los puntos se otorgan de inmediato, sin esperar la moderación. Tres rutas nuevas en `admin.ts` (`GET /admin/pending-contributions`, `POST .../confirm`, `POST .../reject`) reusan `addSourceToRestaurant` sin cambiarla; confirmar agrega un Source real con `kind: 'community'` y `weight: 'weak'` siempre — nunca un peso mayor solo porque un operador aprobó rápido — y `name: 'Un usuario'`, mismo tono que los agregados de comunidad existentes ("42 personas"); rechazar marca el estado, nunca borra. Nueva sección "Aportes de usuarios · pendientes" en el Admin CMS, mismo componente visual de un tap para confirmar que ya usa "Analizar contenido de curador". Verificado con `curl` en ambas direcciones (una nota sobre Osaka confirmada terminó como Source real en `sources[]`; una nota de control sobre Cuervo Café rechazada confirmó que el catálogo no se tocó) y en el navegador real: una tercera nota de prueba sobre Anafe apareció correctamente en la cola con su restaurante, badge de origen ("Nota") y claim real, un click real en "Confirmar y agregar como fuente" la hizo desaparecer de la cola y el nuevo Source (`kind: community`, `weight: weak`) quedó confirmado en el catálogo vía `curl` — el `trustRationale` de Anafe no cambió, correcto, porque la contradicción existente entre dos fuentes más fuertes sigue sin resolverse con una sola fuente débil nueva. Reputación por contribuyente individual y edición del claim antes de confirmar quedan como Open Questions explícitas del spec, sin resolver. Commit `fb62444`.
- **2026-07-07** — Auditoría completa a pedido del usuario ("quiero que la app sea 100% funcional y no tenga data falsa o hardcodeada") de todo `app/` y `brain/` en busca de datos/lógica hardcodeados o placeholders no documentados, seguida de la implementación de lo que el usuario decidió arreglar ahora (dejó el usuario demo fake como está, por decisión explícita, ver el punto en "Estructural" arriba). **Bugs de UI reales corregidos**: el chip de barrio en Living Map mostraba "Palermo" fijo sin `onClick`, sin importar dónde estuviera el usuario — ahora `LivingMap.tsx` calcula el barrio real más cercano por haversine (`lib/geo/geolocation.ts`, nueva función `haversineKm` client-side) contra los restaurantes actualmente cargados, con "Buenos Aires" como fallback honesto cuando no hay ubicación en vez de inventar un barrio; el botón de mic ("Aportar por voz") navegaba a Conversación con un comentario explícito de "no existe todavía" a pesar de que el paso de Voz de Knowledge Capture (SPEC-015) ya era real — ahora abre ese paso directo vía un nuevo `pendingCaptureStage` en el store (mismo patrón "consumido una vez" que `pendingShare`); el overlay de Feedback tenía un default silencioso `restaurantId = 'osaka'` que nunca debería dispararse en producción pero existía igual — se eliminó, `restaurantId` pasó a ser obligatorio y `App.tsx` ya no renderiza el overlay sin uno real; los IDs de turno de conversación (`turn-1`, `turn-2`...) se reseteaban en cada reinicio del server — ahora son `randomUUID()` reales, igual que el resto del proyecto. **Robustez del Brain**: las siete funciones de `claudeClient.ts` devolvían silenciosamente un resultado vacío (`{ restaurantIds: [], reply: '', chips: [] }` y variantes) cuando Claude fallaba o no devolvía texto, haciendo que una falla real de la API se viera como "no hay resultados" en vez de un error — nuevo helper `requireTextBlock()` que tira un error real, propagado por el `try/catch → next(err)` que cada ruta ya tenía, nunca una respuesta falsa disfrazada de éxito. **Datos inventados en el catálogo**: los seis restaurantes semilla tenían una fuente `community` con un conteo de personas inventado ("42 personas", "18 personas", etc.) que aparecía tal cual en el `trustRationale` mostrado al usuario, implicando un consenso real que nunca existió — ahora esa fuente se llama genéricamente "Comunidad", sin un número inventado; el confirmado real vía SPEC-019 sigue usando "Un usuario" para una contribución individual real, dos etiquetas distintas para dos cosas distintas. **Eventos**: el mapa solo tenía un evento hardcodeado en código (`data/restaurants.ts`) sin ninguna forma de agregar uno real, a diferencia de los restaurantes — nuevo `brain/src/data/eventsStore.ts` (mismo patrón JSON-persistido) con `getEvents`/`createEvent`/`deleteEvent`, tres rutas nuevas en `admin.ts` (`GET/POST /admin/events`, `DELETE /admin/events/:id`) y una sección nueva "Eventos" en el Admin CMS con lista + alta + borrado; el label corto ("sáb") ya no se tipea por separado, se deriva siempre del `whenAt` real para que nunca pueda desincronizarse. Verificado con `curl` end-to-end (rationale sin conteos inventados, turn ID como UUID real, crear/listar/borrar evento) y en el navegador real: el mic abrió el paso de Voz real, el chip de barrio mostró el barrio real más cercano tras conceder geolocalización simulada, y un evento creado con un click real en el Admin CMS apareció en la lista y se pudo borrar con otro click real. Commit `0ff2972`.
- **2026-07-07** — Preparación de deploy real (Railway para `brain/`, Vercel para `app/`, a pedido explícito del usuario tras preguntar qué falta para que dos usuarios reales usen la app desde dispositivos distintos). Las seis stores JSON-persistidas (`catalogStore`, `memoryStore`, `curatorStore`, `eventsStore`, `pendingContributionsStore`, `pushStore`) calculaban `DATA_DIR` cada una por su cuenta como una ruta relativa al archivo compilado — funciona en local, pero asume un filesystem escribible en esa ruta relativa, algo que no es cierto en un build de producción sobre un volumen montado en otro lado. Nuevo `brain/src/paths.ts` centraliza `DATA_DIR`, ahora override-able con la variable de entorno `DATA_DIR` (si no está seteada, cae al mismo default relativo de siempre — cero cambio de comportamiento en local, confirmado resolviendo la ruta real con Node antes/después del refactor). CORS pasó de abierto sin condición a restringible: si `ALLOWED_ORIGIN` está seteada, `cors()` se limita a ese origen; si no, sigue abierto (default actual, sin romper nada hoy). Nuevos `brain/railway.json` (build/start explícitos, ya que Railway con Nixpacks no adivina bien la raíz de un servicio dentro de un monorepo) y `app/vercel.json` (rewrite de SPA para que `/conversation`, `/restaurant/:id`, `/share`, etc. respondan con `index.html` en un link directo, no solo navegando desde `/`). Nuevo runbook [deploy.md](deploy.md) con los pasos exactos de Railway/Vercel — variables de entorno, dónde montar el volumen, verificación post-deploy. El deploy en sí (crear los proyectos, cargar los secrets reales, apuntar los dominios entre sí) es una acción sobre las cuentas del propio usuario, fuera de lo que se ejecuta automáticamente acá. Verificado: typecheck limpio en `brain/` tras el refactor, `DATA_DIR` resuelve exactamente al mismo directorio real que antes (confirmado con Node contra el build compilado, y el Brain sirvió el catálogo real de 6 restaurantes sin resiembra tras el cambio), y `npm run build` de `app/` genera un build de producción completo (incluido el service worker vía `injectManifest`) sin errores. Commit `3dceb60`.
